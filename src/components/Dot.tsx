// A small designed separator for meta lines (author · date, title · dept),
// used instead of a typed middle-dot character.
export default function Dot() {
  return <span className="mx-1.5 inline-block h-1 w-1 rounded-full bg-ink-soft/50 align-middle" />;
}
